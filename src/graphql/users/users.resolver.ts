// import {getUserById} from "./users.model";

// const resolvers = {
//   Query: {
//     getUserById: async (_: undefined, { user_id }: { user_id: string }) => {
//       const user = await getUserById(user_id);
//       return user;
//     },
//   },
// };

// export default resolvers;
